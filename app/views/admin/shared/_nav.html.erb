<nav class="bg-indigo-700 shadow-lg">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex">
        <div class="flex-shrink-0 flex items-center">
          <%= link_to admin_root_path, class: "text-2xl font-bold text-white" do %>
            Trego Admin
          <% end %>
        </div>
        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
          <%= link_to "Dashboard", admin_dashboard_path, class: "#{active == 'dashboard' ? 'border-white text-white' : 'border-transparent text-indigo-100 hover:border-indigo-300 hover:text-white'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
          <%= link_to "Drivers", admin_drivers_path, class: "#{active == 'drivers' ? 'border-white text-white' : 'border-transparent text-indigo-100 hover:border-indigo-300 hover:text-white'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
          <%= link_to "Vehicles", admin_vehicles_path, class: "#{active == 'vehicles' ? 'border-white text-white' : 'border-transparent text-indigo-100 hover:border-indigo-300 hover:text-white'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
          <%= link_to "Rides", admin_rides_path, class: "#{active == 'rides' ? 'border-white text-white' : 'border-transparent text-indigo-100 hover:border-indigo-300 hover:text-white'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
          <%= link_to "KYC", admin_kyc_documents_path, class: "#{active == 'kyc' ? 'border-white text-white' : 'border-transparent text-indigo-100 hover:border-indigo-300 hover:text-white'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
          <%= link_to "Complaints", admin_complaints_path, class: "#{active == 'complaints' ? 'border-white text-white' : 'border-transparent text-indigo-100 hover:border-indigo-300 hover:text-white'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
        </div>
      </div>
      <div class="flex items-center space-x-4">
        <span class="text-indigo-100 text-sm"><%= current_user.email %></span>
        <%= link_to "Back to Site", root_path, class: "text-indigo-100 hover:text-white text-sm font-medium" %>
      </div>
    </div>
  </div>
</nav>
