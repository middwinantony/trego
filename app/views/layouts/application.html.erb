<!DOCTYPE html>
<html>
  <head>
    <title>Trego</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <% if notice %>
      <div class="fixed top-4 right-4 z-50 max-w-md">
        <div class="bg-gray-100 border-l-4 border-black text-black p-4 rounded shadow-lg" role="alert">
          <div class="flex items-center">
            <svg class="h-6 w-6 text-black mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <p><%= notice %></p>
          </div>
        </div>
      </div>
    <% end %>

    <% if alert %>
      <div class="fixed top-4 right-4 z-50 max-w-md">
        <div class="bg-gray-100 border-l-4 border-gray-600 text-gray-800 p-4 rounded shadow-lg" role="alert">
          <div class="flex items-center">
            <svg class="h-6 w-6 text-gray-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
            </svg>
            <p><%= alert %></p>
          </div>
        </div>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
